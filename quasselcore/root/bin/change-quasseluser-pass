#!/bin/bash
# vim:set ft=sh noet :

APP_UID=${APP_UID:-1000}
APP_GID=${APP_GID:-1000}

APP_USER=${APP_USER:-appuser}
APP_CONFIG=${APP_CONFIG:-/config}


s6-applyuidgid -u $APP_UID -g $APP_GID /usr/bin/quasselcore --configdir=/config --change-userpass=$1
