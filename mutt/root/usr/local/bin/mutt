#!/bin/bash

USER_UID=${USER_UID:-1000}
USER_GID=${USER_GID:-1000}

APP_USER=${APP_USER:-appuser}

# Invoke GnuPG-Agent the first time we login.
export HOME=/home/${APP_USER}
cd $HOME
s6-applyuidgid -u $USER_UID -g $USER_GID /usr/local/bin/start_mutt
