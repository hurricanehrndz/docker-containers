#!/usr/bin/with-contenv bash
set -e

# Global variables required
APP_UID=${APP_UID:-1000}
APP_GID=${APP_GID:-1000}
APP_USER=${APP_USER:-appuser}

/usr/local/bin/setup-osc

cd ${TARGET_DIR}
s6-applyuidgid -u $APP_UID -g $APP_GID $@ ${extra_opts}
